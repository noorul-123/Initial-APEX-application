CREATE TABLE CICD_TEST_TABLE (
    ID NUMBER GENERATED BY DEFAULT AS IDENTITY,
    NAME VARCHAR2(100),
    CREATED_ON TIMESTAMP DEFAULT SYSTIMESTAMP,
    CONSTRAINT PK_CICD_TEST_TABLE PRIMARY KEY (ID)
);
